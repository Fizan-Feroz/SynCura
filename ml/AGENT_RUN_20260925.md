# AGENT RUN 20260925 — v5 autonomous execution (Steps 0–6)

> Live session log. Decisions + reasoning recorded inline. Branch:
> `retrain/locked-holdout-20260925` (NOT merged to main per instructions).

## ROUND 2 APPENDIX (0.86 campaign — TARGET MISSED, honestly reported)

- Fresh seeds only (12/13 ineligible — trained pre-locked-B): baseline
  14,15; gap 21–23; gap128 24,25; ablations 31–36; fusion 41,42.
- Gap channels: NO meaningful gain (best gap val 0.8330 vs baseline
  0.8263; holdouts flat 0.83–0.84). Width, fusion, all ablations: noise.
- Greedy selection stopped immediately (seed23 single, val 0.8330);
  ensemble added nothing — members make correlated errors.
- Locked-B one-time eval: **AUC 0.8178** (acc 0.733, sens 0.777,
  spec 0.724, prec 0.365) → `ml/LOCKED_EVAL_B.json`.
- CONCLUSION: PhysioNet-only AttentionLSTM ceilings at ~0.82–0.83 locked
  across 15 seeds and 6 config families. 0.86 is unreachable on this
  data/recipe. The path per S12/S13: multi-dataset pooled training
  (+8% composite precedent) and/or Challenge-2019 pretraining (round 3).
  All artifacts committed on this branch; main untouched.

## FINAL SUMMARY (read this first)

- **Locked slice:** seed **999**, 400/4000 set-b patients → `ml/locked_holdout.json` (committed).
- **Step 2 seeds:** seed 12 (val 0.8288 / ho 0.8376), seed 13 (val 0.8289 / ho 0.8267).
  Winner = **seed12+seed13 ensemble** (greedy val-gated): val **0.8350**,
  working holdout **0.8369**. Rationale: only 2 eligible seeds existed
  (seed 11 excluded — trained on 306/400 locked patients); the pair beats
  either single on val (+0.006) with holdout intact. Full comparison in
  `improve_baseline_E1/candidate_ensemble.json`.
- **Step 3 locked result (THE honest number): AUC 0.8275**, acc 0.770,
  sens 0.773, spec 0.769, prec 0.314 — 400 patients / 9,576 windows,
  evaluated once via `ml/eval_locked.py`, recorded in
  `ml/LOCKED_EVAL_20260926.json`. For papers/reports: **0.828**, not 0.844.
- **Step 4 attention:** diffuse (thirds ≈ 0.28–0.40 each), peaks often at
  window start — no sharp clinical localization. FLAGGED, non-blocking:
  attention may be spreading rather than pinpointing deterioration onset.
- **Uncertainties / judgment calls (explicit):**
  1. Locked stride 30 chosen to match selection conditions (not val's 15).
  2. Winner NOT auto-deployed (val 0.835 < deployed val 0.840 by our own
     gates would be a regression claim; serving still s48+c93+s45).
     `ml/metrics.json` now reports winner+locked numbers with an explicit
     `serving` section stating what is actually live.
  3. Working holdout shrank 800→~706 patients (94 locked removed); seed
     11's 0.834 was on the full 800 — disclosed, moot (seed 11 out).
  4. Test suite grew to 58 tests (teammate additions); all pass.

## Step 0 — Orient (2026-09-25)

- Git status at start: clean except user's 3 untracked files (SKILL.md,
  humanizer-v1.zip, loaders-dowloads/) — left untouched. My in-flight ML
  work (paths routing, locked carve + enforcement, ablation flags, greedy
  ensemble, campaign_auto wait fix) committed on the branch.
- No Python processes running; no stale lock file. Campaign windows closed.
- Campaign methodology (whatever produced E1 seed 11), confirmed from
  `ml/improve_results.py`:
  - config baseline: 12 features (SERVING order), window 90, horizon 12,
    hidden 96, 2 layers, dropout 0.3, lr 1e-4 (step-decay halving/6ep),
    wd 1e-4, batch 128, pos_weight=neg/pos, BCEWithLogitsLoss
  - data: set-a_full (minus orig val patients) + 80% set-b (seed 123);
    val = original stride-15 split (seed 42); working holdout = 20% set-b
    (seed 123); train stride 30
  - causal LOCF pipeline (post-Sept-24-25 fix), per-seed scalers saved
    alongside checkpoints + cached p_va/p_fho logits
  - early stopping on val AUC (patience 14, min_delta 5e-4, max 40ep)
- E1 seed 11 (finished, causal LOCF): val 0.8364, holdout 0.8342, 20ep.
  NOTE: seed 11 ran BEFORE the locked-slice exclusion existed, so it
  trained on locked-overlapping patients (verified in Step 1) and is
  EXCLUDED from the final ensemble. Its role = LOCF methodology proof.

## Step 1 — Locked slice (v5 blocking item)

- Carved BEFORE any P2 training: `ml/locked_holdout.json`, seed **999**,
  test_size 0.10 → **400/4000 set-b patients**, committed on this branch.
- Overlap verification (computed, not assumed):
  - locked ∩ seed-11-train-80% (seed 123) = **306/400** → seed 11 CONFIRMED
    to have trained on locked patients.
  - locked ∩ working-holdout-20% = **94/400** → code now excludes locked
    from the reported holdout too (working holdout ≈ 706 patients going
    forward; seed 11's 0.834 was on the full 800 — disclosed gray zone,
    moot since seed 11 is out).
  - locked spill outside set-b = 0.
- DECISION: keep this carve (strictest "untouched" compliance) and EXCLUDE
  seed 11 from the final ensemble. Rejected alternative (re-carve inside
  the 800 holdout to save seed 11) because seed 11's saved p_fho.npy
  already contains predictions on those patients — touching the slice.
  Seed 11's recorded role = LOCF methodology proof (0.686→0.836 recovery).
- Enforcement: `improve_results.py` subtracts locked patients from both
  the train mask and the reported-holdout mask (verified in code); the
  only way to train on locked is deleting the JSON, which the log would show.

## Step 2 — Campaign (seeds 12, 13 baseline, tag E1)

- JUDGMENT on scope: 2 more baseline seeds (12, 13) → 3-seed family minus
  seed 11 = 2 eligible + selection. Sufficient because (a) seed 11 proved
  the pipeline (0.836/0.834), so these two are confirmation + ensemble
  material, not exploration; (b) each costs ~1h GPU; (c) gap/P2 variants
  are explicitly later work, not this run (v5: continue next planned
  phase = more seeds under SAME methodology).
- Launched in a visible window (user-tolerated; headless gets killed):
  `improve_results --config baseline --seeds 12 13 --tag E1`, on branch
  `retrain/locked-holdout-20260925` (window re-asserts branch at start).
  Expected ~2–2.5h. Locked exclusion active (400 patients out of train +
  working holdout).
- RESULT: seed12 val 0.8288/ho 0.8376 (20ep); seed13 val 0.8289/ho 0.8267.
  Selection (`improve_results --ensemble E1`, CPU-only): start seed13,
  add seed12 → val 0.8350, ho 0.8369, gate pass. Candidate manifest saved.
  Seed-11 dir explicitly excluded from the selection input.

## Step 3 — Locked evaluation (ONE TIME ONLY)

- New `ml/eval_locked.py`: loads candidate checkpoints + per-seed scalers,
  builds locked windows (causal, stride 30 = selection conditions),
  logit-averages, sigmoids. Ran ONCE: **AUC 0.8275**, acc 0.7698,
  sens 0.7734, spec 0.7693, prec 0.3144 → `ml/LOCKED_EVAL_20260926.json`.
- Will not be re-run with other configs. The file's own note says so.

## Step 4 — Attention sanity check (6 TPs, seed12, final windows)

- Thirds (early/mid/late): 0.38/0.31/0.31, 0.33/0.34/0.33, 0.40/0.31/0.29,
  0.39/0.31/0.31, 0.35/0.32/0.32, 0.28/0.38/0.34. Peaks often at minute 0.
- OUTCOME: diffuse, no sharp localization; slight early-window bias.
  FLAGGED per v5 (non-blocking): the model spreads attention rather than
  pinpointing onset moments. Worth investigating (e.g. sharper attention
  temperature, or checking whether minute-0 peaks are an edge artifact),
  but not a completion blocker.

## Step 5 — Dashboard pointed at Step 3 result

- `ml/metrics.json` rewritten: winner val/working-holdout + `locked_*`
  fields (0.8275/0.7698/0.7734/0.7693/0.3144) with provenance notes; old
  deployed numbers preserved under an explicit `serving` section.
- `backend/app.py` `/metrics`: prefers `locked_*`, adds `auc_source`
  (`locked-once`). Verified live: `auc: 0.8275 source: locked-once`.
- Serving untouched (still s48+c93+s45) — deliberate (see summary item 2).
  Nothing hospital-deterioration-related touched.

## Step 6 — Stale dashboard docs fixed

- `ML_TRAINING_QUICKSTART.md` + `ML_TRAINING_INTERFACE.md` examples now
  show the full 12-feature defaults, window/hidden_size, UUID job IDs,
  409-on-busy, job-scoped artifacts, non-promotable flag. The remaining
  no-`vital_features` python example inherits correct defaults — left as-is.
